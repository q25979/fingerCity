<view
  class="single-list"
  wx:for="{{ list }}"
  wx:key="{{ index }}"
  data-index="{{ index }}"
  bindtap="selectionTap"
>
  <view class="title"> {{ item.position }} </view>
  <view class="point"> > </view>
</view>